# レビューフィードバック (2026-05-31)

### 実装の評価
全体の構造が非常にクリアで、特に `AsyncSeriesWaterfallHook` の非同期ループ（バケツリレー）のロジックが正確に実装できていました。

### さらなる改善のためのポイント

#### 1. 意味論に応じたメソッド名の使い分け
- **同期フック**: `promise` ではなく `call` を使うのが一般的です。これは「戻り値を待つ必要があるか（Promiseか）」を使う側に明示するためです。
- **登録メソッド**: 同期は `tap`、非同期は `tapPromise` と分けることで、フックの性質をより明確にできます。

#### 2. 戻り値の扱いの違い
- **SyncHook / AsyncSeriesHook**: これらは「通知」が目的であり、戻り値によるデータの加工は行いません。そのため、`currentData = ...` のような上書きは不要です。
- **WaterfallHook**: これだけが戻り値を次の入力に使う「加工」の役割を持つます。

#### 3. 可変長引数のサポート
`...args` (スプレッド演算子) を活用することで、コンストラクタで定義した複数の引数（例：`["data", "user", "options"]`）を正しくプラグインに渡せるようになります。

---

# レビューフィードバック (2026-06-01)

### 実装の評価
リファクタリングにより、設計意図が明確で非常に見通しの良いコードになりました。
`AsyncHookBase` の導入による共通化や、`PluginError` による詳細なエラー情報の付与など、実用性を意識した素晴らしい実装です。

### 良かったポイント
1.  **AsyncHookBase による整理**: 同期と非同期のセマンティクスを明確に分けつつ、`tapPromise` を共通化できています。
2.  **エラーハンドリングの強化**: `originalError` や `pluginName` (メッセージ内) を保持し、Fail-Fast な設計を採用したことで堅牢性が向上しました。
3.  **Waterfall の引数管理**: 第1引数（加工対象）とそれ以外の引数を正しく分離して扱えています。

### さらなる改善のためのポイント

#### 1. データ構造の選択 (Map vs Array)
- 現在 `Map` を使用していますが、同じ名前のプラグインが複数登録された場合に上書きされてしまいます。
- `[{ name, fn }, ...]` のような配列形式にすることで、同名プラグインの共存と確実な実行順序の保証が可能になります。

#### 2. エラーハンドリングの DRY 化
- 各クラスの実行ループ内にある `try...catch` のロジックが重複しています。
- 基底クラスに `_runTap(name, fn, args)` のようなラッパーメソッドを作ることで、エラーハンドリングを一箇所に集約できます。

#### 3. 非同期実行の共通化
- `AsyncSeriesHook` と `AsyncSeriesWaterfallHook` のループ構造は似ているため、さらなる抽象化の余地があります。

---

# レビューフィードバック (2026-06-01) - 最終

### 実装の評価
アドバイスを的確に反映し、ライブラリとして完成度の高いコードに仕上がりました。
特に `_runTap` による抽象化は、コードの可読性とメンテナンス性を劇的に向上させています。

### 最終的な改善ポイント
1.  **データ構造の最適化**: `Map` から `Array` への変更により、プラグインの共存と実行順序が保証されました。
2.  **実行ロジックの集約**: `AsyncHookBase` でのエラーハンドリング共通化により、各フックの責任範囲が明確になりました。
3.  **一貫したインターフェース**: `tap` / `tapPromise` / `call` / `promise` の使い分けが洗練され、外部から利用しやすい設計です。
