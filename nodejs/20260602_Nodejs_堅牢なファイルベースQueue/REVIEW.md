# exercise: Node.js 永続化キューの実装 (2026-06-02)
## 2026-06-02 初回レビュー結果
実装お疲れ様です！アトミックなファイル操作が適切に実装されています。
明日のリファクタリングに向けて、以下のポイントを整理しました：

1.  **境界条件のハンドリング**:
    - キューが空の状態で `dequeue()` を呼んだ際の挙動（現在は `undefined` を返却しファイルは空配列で更新）。
2.  **環境のセットアップ**:
    - `./queues` ディレクトリが存在しない場合にエラーになるため、自動作成（`mkdir -p`）の導入。
3.  **堅牢なリカバリ**:
    - `recovery()` 実行時にファイルが存在しない（初回起動時）場合のエラーハンドリング。
4.  **型定義の厳密化**:
    - `writeQueueFile` の引数型やメンバ変数の `any` 回避など、TypeScriptとしての品質向上。

## 明日の予定
- 上記指摘事項のリファクタリング実施
- エラーハンドリングの強化
- テストケースの追加（境界条件など）

## 2026-06-03 本日の取り組み
1. キューが空の状態でdequeueを読んだ際の挙動
 undefined が返ったときキューに入っていたのか、空だったからなのか区別できないため、例外を投げる

2. queuesディレクトリが存在しない場合作成する

3. recovery()実行時のエラーハンドリング
 ファイルが存在しなかった場合、異常終了してしまうのを修正

4. 型定義の厳格化
 ジェネリック型で要素の型を指定

## 2026-06-03 フィードバック
修正お疲れ様です！要件がしっかりと反映されており、特に独自例外（QueueEmptyError）の導入や、`static` メンバを用いた名前重複チェックなどの工夫が素晴らしいです。

さらに堅牢で使いやすいライブラリにするためのアドバイスをまとめました：

1.  **静的ファクトリメソッドの実装**:
    - 追加された `create` メソッドがインスタンスメソッドになっています。また、内部で `recovery()` を `await` していないため、初期化が完了する前にインスタンスが返ってしまいます。前述の「静的ファクトリメソッド」パターンへの修正を検討してみてください。
2.  **ディレクトリ作成の最適化**:
    - `fs.access` -> `fs.mkdir` の順で実行すると、チェックと作成の間に別の処理が挟まる「TOCTOU」という脆弱性や race condition の可能性があります。
    - `await fs.mkdir(dir, { recursive: true })` を直接呼ぶのが Node.js におけるベストプラクティスです（既に存在していてもエラーになりません）。
3.  **カプセル化（private）**:
    - `_queue` や `_tmpFile` など、外部から書き換えられたくないメンバ変数は `private` 修飾子を付けることで、クラスの整合性を守ることができます。
4.  **型の整合性**:
    - `peek()` の戻り値が `any` になっています。`T | undefined` とすることで、利用側が安全に型チェックを行えるようになります。
5.  **テストコードの有効化**:
    - `QueueEmptyError` のテストがコメントアウトされているため、有効化して正常に動作することを確認しましょう。

## 2026-06-04 本日の取り組み
- ファクトリメソッドの不具合修正
  インスタンスメソッドになっていたのをstaticに修正
  recoveryをawaitしていなかった(ファクトリメソッド実行に続く同期処理が先に走ってしまう）  
- ディレクトリ作成時の存在チェックに置けるrace condition解消
  access -> 作成　となっていたのを、直接作成するよう修正(mkdirのオプションに recursive: true を指定)
- encapsulation（アクセス修飾子）の整理
- テストケース追加
  QueueEmptyErrorのテスト実施
- メソッド戻り型エラー解消

## 2026-06-04 フィードバック
修正お疲れ様です！設計が洗練され、特に TypeScript の機能を活かした改善が見られます。
さらなる品質向上のための仕上げとして、以下のポイントを確認してみてください：

### 1. 正確性 (Accuracy)
- **致命的なタイポ**: `constructor` 内で `this,_queueDir` となっている箇所があります。カンマをドットに修正が必要です。
- **リカバリ時の例外処理**: `recovery()` 内の `catch` ブロックが空のため、JSONが壊れている場合も正常系として処理が進んでしまいます。
- **TOCTOUの排除**: `fs.access` -> `fs.readFile` ではなく、直接 `readFile` を試行し、エラーハンドリングを行う方がより安全です。

### 2. 可読性 (Readability)
- **コンストラクタの隠蔽**: `static create` を導入したので、`constructor` を `private` にすると、外部から不完全なインスタンスが作られるのを防げます。
- **冗長なキャスト**: `fs.readFile(..., 'utf-8')` の戻り値に対する `as string` は不要です。

### 3. 効率性 (Efficiency)
- **テストの整合性**: テストコード側でも `new` ではなく `PersistentQueue.create()` を使うように書き換えると、ライブラリとしての正しい使い方の証明になります。

## 2026-06-04 今回の取り組み
- [X] タイポの修正、
- [X] コンストラクタのprivate化
- [X] recovery中のTOCTOUの排除
- [X] テスト中のインスタンス生成をcreateで行う

## 2026-06-05 フィードバック (Append-only Log 実装)
修正お疲れ様です！「Append-only Log + Byte Offset」の概念を、非常に短いスパンでコードに落とし込めましたね。
特に、`fs.writeFile` の `flag: 'a'` を使った追記や、`spos`（start position）による論理削除の実装は、まさに永続化キューの進化の核心を突いています。

### 1. 正確性 (Accuracy)
- **NDJSONパース時の末尾問題**: `recovery()` で `split("\n")` を使用していますが、ファイル末尾の改行により最後の要素が空文字になり、`JSON.parse` で失敗する可能性があります。
- **Manifest更新の原子性**: `dequeue` 時に直接 `manifestFile` を上書きしていますが、クラッシュ時にファイルが破損するリスクがあります。以前の `rename` 方式（Atomic Write）の適用を検討してください。

### 2. 可読性 (Readability)
- **バイト数計算の共通化**: メッセージ長 + 1（改行）の計算を `enqueue` と `dequeue` で共通化すると、将来のフォーマット変更に強くなります。

### 3. 効率性 (Efficiency)
- **巨大ファイルへの対応**: `recovery()` でファイル全体を `readFile` しています。ログが巨大化した際にメモリ不足になるため、`spos` を利用して必要な範囲だけをストリーム等で読み込むのが理想です。

## 次のステップへのヒント
- `recovery` における「全読み込み」を、`FileHandle.read` や `createReadStream` の `start` オプションを利用した「必要な範囲のみの読み込み」に改善してみましょう。
- 溜まり続けるログをいつか掃除する「ログの圧縮（Compaction）」についても考えてみてください。

## 2026/06/05 本日の取り組み
キューファイルの効率化
登録ごとにデータの洗い替え　→ 追加した分のみ書き込み

### 使用するファイル
{キュー名称}.manifest
 キューの先頭のバイト位置を記録
{キュー名称}.queue
 キューのデータを記録。中身は改行区切りのJSON(NDJSONというらしい)

### enqueue/dequeue/recovery の挙動
- enqueue:
  引数で受け取った値をJSON.stringifyしたものを末尾に追加したうえで行末に改行を追加
- dequeue:
  manifestから取得した先頭位置から改行までのバイト数をmanifestに足しこむ(論理削除）
- recovery
  manifestから読み取った先頭位置からqueueファイルの末尾までの各行をJSON.parseしてメモリ上の配列にpush
